services:
  api:
    build: .
    ports:
      - "8000:8000"
    env_file:
      - .env.example
    environment:
      AI_COPILOT_DATABASE_URL: sqlite:////data/copilot.db
    volumes:
      - copilot-data:/data

volumes:
  copilot-data:

